worker_processes 1;

error_log stderr warn;
pid /var/run/nginx.pid;

events {
  worker_connections  1024;
}

http {

  map $http_upgrade $connection_upgrade {
    default upgrade;
    '' close;
  }

  upstream command {
    server sah-mvp-api:8081;
  }

  upstream frontend {
    server sah-mvp-frontend:4200;
  }

  server {
    listen 80;
    server_name localhost;

    location /api/ {
      proxy_redirect off;
      proxy_set_header Host $host;
      proxy_pass http://command;
    }

    location /  {
      proxy_redirect off;
      proxy_set_header Host $host;
      proxy_pass http://frontend;
    }
  }
}